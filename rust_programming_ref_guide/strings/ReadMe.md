In Rust, they are usually found in two forms: 

      1. the &str type (pronounced stir) 
      2. the String type. (String::from("xyz"))

Rust strings are guaranteed to be valid UTF-8 encoded byte sequences.
